apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-deployment
  namespace: dev
  labels:
    app: client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: client
  template:
    metadata:
      labels:
        app: client
    spec:
      containers:
      - name: client
        image: ayaan49/client:latest1
        ports:
        - containerPort: 80

